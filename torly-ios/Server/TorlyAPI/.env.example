# Generate a random hex password. Never commit the real .env.
POSTGRES_PASSWORD=replace-with-random-64-character-hex
API_DOMAIN=api.example.com
# For local CLI tools; compose supplies DATABASE_URL itself.
# DATABASE_URL=postgresql://torly:password@127.0.0.1:5432/torly
